# Global leaderboard config (gitignored). See .env.example and worker/README.md.
#
# Fill this in with the workers.dev URL `npm run deploy` prints from worker/.
# Left blank, the global board disables itself and the local Hall still works.
VITE_LEADERBOARD_API=https://order-of-order-leaderboard.alexblackburn32.workers.dev
VITE_LEADERBOARD_SUBMIT_KEY=pls-submit-my-score
